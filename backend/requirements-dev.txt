-r requirements.txt

# ── 測試 ─────────────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
eval_type_backport>=0.2      # Python 3.9 compat for X | Y annotations in FastAPI
python-multipart>=0.0.27     # required for FastAPI Form/File in tests
watchdog>=4.0                # restart celery worker on code changes in Docker dev
